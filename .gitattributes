# Normalize line endings so weekly regenerations don't create noisy diffs.
# Text files are stored with LF in the repo regardless of the OS that wrote them.
* text=auto eol=lf
*.html text eol=lf
*.py    text eol=lf
*.md    text eol=lf
*.toml  text eol=lf
