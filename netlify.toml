# Netlify config — static site, files served from the repo root.
[build]
  publish = "."
  command = ""

[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"

[[redirects]]
  from = "/talent.json"
  to = "/.netlify/functions/talent-feed"
  status = 200
  force = true
